# Adding a Rule

Rules should be transparent, editable, and testable.

A rule should define:

- supported variable types
- parameters
- plain-language rationale
- citation keys
- default action
- review requirements
- renderer support expectations

Rules must not be embedded directly in UI components.

Phase 5 will define the rule configuration format.
